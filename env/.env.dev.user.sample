# Sample Environment Configuration for Azure Production Deployment
# Copy this file to .env.dev.user and fill in your values
# WARNING: .env.dev.user is gitignored - never commit secrets!

# ============================================================================
# OpenAI Configuration (REQUIRED)
# ============================================================================
# For production, use Azure Key Vault instead of storing directly
# Get your API key from: https://platform.openai.com/api-keys
SECRET_OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ============================================================================
# Azure Bot Service Configuration (Set by Teams Toolkit during provision)
# ============================================================================
# These are automatically populated during 'provision' command
# BOT_ID=
# TEAMS_APP_ID=
# BOT_AZURE_APP_SERVICE_RESOURCE_ID=
# BOT_DOMAIN=

# ============================================================================
# Azure Subscription (REQUIRED for Azure deployment)
# ============================================================================
# Your Azure subscription ID
# AZURE_SUBSCRIPTION_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Resource group name for the bot
# AZURE_RESOURCE_GROUP_NAME=rg-m365-admin-bot-prod

# Unique suffix for globally unique resource names
# RESOURCE_SUFFIX=abc123

# ============================================================================
# Microsoft App Registration (Set during bot registration)
# ============================================================================
# MICROSOFT_APP_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# MICROSOFT_APP_PASSWORD=your-client-secret-here
# MICROSOFT_APP_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ============================================================================
# Optional: AI Model Configuration
# ============================================================================
# OPENAI_MODEL_NAME=gpt-4o
# OPENAI_TEMPERATURE=0.7
# OPENAI_MAX_TOKENS=2000

# ============================================================================
# Optional: Production Settings
# ============================================================================
# Enable Application Insights for monitoring
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=xxx

# Rate limiting (requests per minute per user)
# RATE_LIMIT_RPM=30

# Enable detailed logging (not recommended for production)
# DEBUG_MODE=false

# Maximum conversation history
# MAX_CONVERSATION_HISTORY=20

# ============================================================================
# Optional: Microsoft Learn MCP
# ============================================================================
# MICROSOFT_LEARN_ENABLED=true

# ============================================================================
# Optional: Azure Key Vault (Recommended for production)
# ============================================================================
# Store secrets in Azure Key Vault instead of environment variables
# AZURE_KEY_VAULT_NAME=kv-m365-admin-bot
# Use managed identity or service principal to access Key Vault

# ============================================================================
# Notes:
# ============================================================================
# 1. This file is for PRODUCTION DEPLOYMENT to Azure
# 2. Never commit .env.dev.user to version control
# 3. Consider using Azure Key Vault for secret management
# 4. Use managed identity instead of connection strings when possible
# 5. Enable Application Insights for production monitoring
# 6. Set up proper RBAC permissions in Azure
# 7. Configure rate limiting to prevent abuse
# 8. Regular key rotation every 90 days
# ============================================================================
